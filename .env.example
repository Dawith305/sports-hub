# Copy to `.env` and adjust if needed before running the app (`pnpm dev`).

VITE_THESPORTSDB_API_BASE=https://www.thesportsdb.com/api/v1/json/3

# TheSportsDB league id for upcoming fixtures
VITE_DEFAULT_LEAGUE_ID=4328
